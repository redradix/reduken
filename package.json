{
  "name": "reduken",
  "version": "1.0.25",
  "description": "Redis style structures as redux reducers, actions and selectors",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/redradix/reduken.git"
  },
  "scripts": {
    "test": "jest",
    "test:dev": "jest --watch",
    "test:coverage": "jest --coverage",
    "type-check": "tsc --noEmit",
    "type-check:watch": "npm run type-check -- --watch",
    "prebuild": "rm -rf lib",
    "build": "npm test && npm run build:js && npm run build:types",
    "build:types": "./generate-types.sh",
    "build:js": "babel src -d lib --extensions \".ts,.js\" --minified --ignore src/**/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "reducer",
    "redux",
    "redis",
    "hash"
  ],
  "jest": {
    "testURL": "https://www.redradix.com"
  },
  "author": "redradix",
  "license": "ISC",
  "dependencies": {
    "ramda": "^0.26.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/plugin-proposal-class-properties": "^7.4.0",
    "@babel/plugin-proposal-object-rest-spread": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "@babel/preset-typescript": "^7.3.3",
    "babel-jest": "^24.7.1",
    "jest": "^24.7.1",
    "typescript": "^3.4.3"
  },
  "bugs": {
    "url": "https://github.com/redradix/reduken/issues"
  },
  "homepage": "https://github.com/redradix/reduken/#readme"
}
